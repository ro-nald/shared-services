# Terraform working directories and generated files
.terraform/
*.tfstate
*.tfstate.backup
tfplan.binary
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Per-developer local variable overrides (project config lives in *.auto.tfvars instead)
terraform/platform/core/terraform.tfvars
terraform/platform/iam/terraform.tfvars
terraform/environments/dev/terraform.tfvars

# Backend config files — generated per-developer by the bootstrap CLI, never committed
**/backend.hcl

# Bootstrap CLI state — written by `uv run scripts/bootstrap.py apply-core`
scripts/.bootstrap-state.json

# Secrets and credentials — never commit these
.env
.env.*
*.pem
*.key
*.p12
*.pfx

# macOS
.DS_Store

# Editor
.idea/
.vscode/
*.swp
